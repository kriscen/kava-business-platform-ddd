---
name: update-docs-map
description: 扫描 docs/ 目录下所有文件，自动生成或更新 docs/00-project-map.md 文档地图。当 docs 目录内容发生变化（新增、删除、重命名文件）后使用此 skill 同步索引。
license: MIT
compatibility: 无特殊依赖。
metadata:
  author: Kris
  version: '1.0'
  generatedBy: '1.0'
---

扫描 `docs/` 目录，根据实际文件内容更新 `docs/00-project-map.md`。

**步骤**

1. **扫描目录结构**

   使用 Glob 工具递归列出 `docs/` 下所有文件（排除 `00-project-map.md` 自身）：
   ```
   docs/**/*
   ```

   按编号前缀整理子目录顺序（`01-sql/`、`02-architecture/` 等）。

2. **读取每个文件的前 30 行**

   对每个文件使用 Read 工具读取前 30 行，提炼其用途。

   提炼规则：
   - SQL 文件：提取数据库名、表数量、主要表名
   - Markdown 文件：提取标题、摘要描述
   - YAML/JSON 文件：提取关键定义（如 OpenAPI 版本、认证方式）
   - 其他文件：根据内容给出简短说明

3. **读取现有 project-map**

   读取 `docs/00-project-map.md` 的当前内容（如存在），了解已有格式。

4. **生成新的 project-map**

   按以下模板生成新内容：

   ```markdown
   # 项目文档地图

   本文档是 `docs/` 目录的导航索引，帮助你快速找到所需文档。

   ## 目录结构

   ```
   docs/
   ├── 00-project-map.md        ← 你在这里
   ├── 01-sql/                  # 一句话描述
   ├── 02-architecture/         # 一句话描述
   ...
   ```

   ---

   ## 01-xxx/ — 分类名称

   | 文件 | 说明 |
   |------|------|
   | [filename](01-xxx/filename) | 一句话描述该文件内容和用途 |

   ---

   ## 快速导航

   **场景名称**：`文件A` → `文件B` → `文件C`
   ```

   生成规则：
   - 使用中文编写所有描述
   - 每个子目录一个章节，标题格式为 `## NN-xxx/ — 分类名称`
   - 每个文件在表格中占一行，包含相对链接和简短说明（一句话）
   - 末尾的「快速导航」按常见使用场景提供阅读路径
   - 保持简洁，说明控制在 50 字以内

5. **写入文件**

   使用 Write 工具将生成的内容写入 `docs/00-project-map.md`。

6. **显示摘要**

   输出更新摘要：
   - 列出新增的文件条目（相比旧版本）
   - 列出删除的文件条目（相比旧版本）
   - 总文件数

   格式：
   ```
   ## 文档地图已更新

   **总文件数**: N
   **新增**: +file-a.md, +file-b.md
   **移除**: -file-c.md
   ```

   如果没有任何变化，输出：`文档地图已是最新，无需更新。`

**护栏规则**

- 始终排除 `00-project-map.md` 自身，不将其列入文件表
- 不要修改 docs/ 目录下的任何其他文件
- 文件说明必须基于实际内容，不要猜测
- 如果子目录为空，在目录结构树中标明，但不创建空的章节表格
- 保持编号前缀的升序排列
