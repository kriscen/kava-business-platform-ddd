server:
  port: 8500

spring:
  application:
    name: @artifactId@
  profiles:
    active: dev
  cloud:
    nacos:
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        server-addr: localhost:8848
        namespace: 92c33821-3fa9-4f51-92d3-6fd27ee9dbf4
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: ${spring.cloud.nacos.discovery.namespace}
  config:
    import:
      - optional:nacos:application-${spring.profiles.active}.yml
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.yml

# =====================================================================
# 生产环境 CORS 配置模板 (通过 Nacos: kbpd-gateway-prod.yml 注入)
# =====================================================================
# spring.cloud.gateway.globalcors.cors-configurations.'[/**]'.allowed-origins:
#   - https://admin.kava.example.com
#   - https://app.kava.example.com
# spring.cloud.gateway.globalcors.cors-configurations.'[/**]'.allowed-methods:
#   - GET
#   - POST
#   - OPTIONS
# spring.cloud.gateway.globalcors.cors-configurations.'[/**]'.allowed-headers:
#   - Authorization
#   - Content-Type
# spring.cloud.gateway.globalcors.cors-configurations.'[/**]'.max-age: 3600